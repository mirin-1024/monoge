<% unless current_user?(@user) %>
  <section class="follow-form">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </section>
<% end %>
