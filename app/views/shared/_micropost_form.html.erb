<%= form_with(model: @micropost, local: true) do |f| %>
  <% render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "投稿を作成" %>
  </div>
  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>
